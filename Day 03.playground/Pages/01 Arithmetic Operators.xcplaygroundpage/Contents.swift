// Arithmetic Operators

import UIKit

let numX = 7
let numY = 3

let soma = numX + numY
print(soma)
let subtracao = numX - numY

let multiplicacao = numX * numY
let divisao = numX / numY

let rest = numX % numY
